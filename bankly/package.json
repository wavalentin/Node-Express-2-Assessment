{
  "name": "bankly",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "seed": "dropdb bankly --if-exists && createdb bankly && psql bankly < data.sql && dropdb bankly_test --if-exists && createdb bankly_test && psql bankly_test < data.sql",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.16.4",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "supertest": "^3.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
